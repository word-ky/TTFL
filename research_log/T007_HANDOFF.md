# T007 active — identity prerequisite first

Lead 3394d2c read in full at 2026-09-13 19:32 +08. No TTFL job active. Existing remote 24 tests pass. Reuse map: PFLlib read_data and split manifest; unchanged ContextFedAvgCNN/checkpoint; unchanged paired_clean_oracle; unchanged corruption utility; existing evaluator/hash/CSV helpers. New work only deterministic balanced pool selection, exact pairing controls, pooled state transfer and macro-class reporting.

Increment 1: balanced_pool.py plus 2 focused stdlib tests PASS locally. Exact 80/pool, 8/class, disjoint original IDs, maximize distinct clients via prescribed SHA ordering; labels restricted to selection/pairing audit. Increment 2 preflight: real pools, exact tensor/feature multiset checks, two clean-identity fits then all100 clean query evaluation. This is required before interpreting shifts because T004 recorded low-variance coefficient shrinkage with fixed epsilon. No new numerical identity coefficient threshold: record full state and function differences, diagnose any material deviation under Lead's explicit prerequisite. No corruption states yet. Formal transfer implementation follows only if prerequisite is scientifically satisfied. No T008.
