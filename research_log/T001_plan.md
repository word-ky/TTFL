# T001 fixed first experiment

Read/operator feasibility only; supervised diagnostic, no SSL/meta/FL. One newly trained backbone seed7, source MNIST/USPS/MNIST-M, target SVHN. 6000 random training images/source, Adam0.001, batch128,20 epochs, final epoch checkpoint chosen without target evaluation. Small CNN 3 Conv-BN-ReLU-Pool blocks32/64/128, linear head. Original implementation using torch modules; torchvision0.19 loaders (BSD-3-Clause library, imported not copied). MNIST-M image mirror: https://github.com/mashaan14/MNIST-M . Preprocess all to RGB32x32 and [-1,1], no augmentation. MNIST-M mirror has independent provenance, do not claim exact reproduction of Claude's data.

Query: fixed3000 SVHN official test examples, seed101. Five support draws from official training splits, seeds11/22/33/44/55;20/class. Wrong support independently drawn from each of3 source domains, same class counts. Shuffled support permutes labels of exactly the correct images; noise replaces images by uniform[-1,1] with same labels. Per-episode random affine initialization std0.01 without write. Every method starts with the same final checkpoint, every affine episode starts at zero, BN remains eval during supervised updates; fast state448 parameters. No baseline training data uses SVHN. Wrong support may overlap source training (disclosed) but never target query. Correct/shuffled/noise and wrong supports all share equal class counts to control priors.

Methods: no adaptation; BN replacement using whole support batch/momentum1, each context; label-prior reweighting with Laplace1 support prior / source training prior; fast affine grid3LRx4steps (supplied grid), all contexts; optional full-backbone SGD lr.001 steps1/3/5/10, correct support only, BN eval. Record raw predictions, support/query CE, class accuracy, fast-state norms, config/source/file hashes/IDs/environment. No gradients from queries. No query-based checkpoint selection. The grid is explicitly exploratory on this query set: report every cell, success is provisional read-operator evidence on one checkpoint, never confirmatory generalization or trained-seed replication.

Acceptance uses mean over five support draws at a SINGLE shared grid cell: gain>=5pp and correct-minus-EACH-wrong and correct-minus-shuffled>=2pp. Report noise/random/BN/prior without hiding negatives. No requirement that this supervised diagnostic beat BN added to the lead's gate; comparison determines framing. If gate fails, stop and return to Lead; if passes, still await T002 assignment.

Increment1 baseline: one training update, exact save/load/logits, evaluator test PASS. Increment2 affine/BN: neutral logits<1e-6, frozen backbone+BN, reset and shared state tests PASS. Increment3 data/runner: synthetic split/preprocess plus real-data smoke before full run. Final full run on physical A6000 GPU0; project-local and remote receipts retained.
