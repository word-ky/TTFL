# T024 semantic-state geometry — protocol freeze

Lead d55a0f9. Clean visual context only, privileged semantic compositions, anonymous fixed neutral operators s0..s4. s0 is the inherited strict no-op. Reuse T014/T013 frozen artifacts without model inference, training, SSL, writer, new state or federation. This is an oracle-template diagnostic, not a deployable method.

800 source compositions indexed by salt×client×train_half, paired to1600 target-excluded templates by bank. Exact pi0 is class_counts/sum_counts, verified against saved T013 half counts. All source counts were inspected before utility access:800 vectors, zero uniform degenerates. Hash all T014/T013 manifests and every input; validate original freezes, cardinalities, state order and positive class denominators. Any mismatch is T024-I and stops without a mechanism verdict.

For offset k=1..9 define right-cyclic q[c]=pi0[(c-k)mod10]. Select maximum exact TV, breaking ties by minimum k. Degenerate uniform vectors retain q=pi0 and are flagged; do not invent another target. Fixed lambdas1/4,1/2,3/4,1; pi_lambda=(1-lambda)pi0+lambda q; exact TV scaling is asserted. JS means square root of Jensen-Shannon divergence with base2 logarithms (range0..1), descriptive only.

Within each salt/train_half, sort client IDs by full SHA256(`T024|pair|salt|train_half|client`) bytes ascending. Each client is paired to the next in the sorted list, wrapping once. This is a bijective, self-excluding mapping of100clients. Mismatch uses the paired client's pi_lambda under the target's same template. Lambda is matched; realized TV magnitudes need not be identical across different source compositions. Freeze all vectors, offsets, distances and pairings before loading template utility values.

T014 template entries are exact class accuracy differences relative to no-op, not absolute class accuracies. Use clean-context entries only, preserving this baseline-relative representation; the shared pi-dependent baseline cancels in oracle choices and every stale/mismatch regret. Reconstruct all1600 clean templates from other99clients' saved T013 class counts and correct counts. Label candidate columns only s0..s4 in new receipts/reports.

For each target cell and lambda, evaluate pi0/current/paired-current using exact Fraction dot products. Retain all utility vectors, full argmax sets and first canonical maximizers. Score current/historical/mismatch/no-op on the correct pi_lambda. Save stale/mismatch regrets, relative-zero utilities, switch bit, argmax overlap and top-two margin. Expected6400 rows:800compositions×2banks×4lambdas. Reports use percentage points for accuracy differences, with exact fractions retained.

Promotion aggregates use nondegenerate cells (all800source compositions are nondegenerate here); retain all raw rows. At lambda1, HEADROOM needs bothbanks meanstale>=.010, fractionstale>=.010 at least.30, and switchrate>=.30. SPEC needs eachbank>=3/4salts meanmismatch>=.005 and pooledfractionmismatch>=.010 at least.25. TREND needs eachbank nondecreasing meanstale (tolerance1e-12) and severe-minus-mild>=.005. ZERO safety is descriptive. All gate comparisons use exact fractions, including rational1e-12 tolerance; only summaries/JS/Spearman become floats.

Report bank×lambda means/median/p75/p90/distributions, exact state transitions, mismatch-by-salt, zero-state safety and Spearman TV/JS versus stale regret over cell×lambda rows (repeated observations, not independent trials). Nondecreasing stale regret is partly algebraic for a fixed-bank linear mixture with an optimal historical state; the magnitude gate is the substantive requirement.

Outcomes: S allthreepass; H headroompass butotherfail; N headroomfail; Iintegrity/replayfailure. Separate verifier independently reconstructs every vector, pairing, clean template, utility, choice/argmax, regret, bit, row count and gate without importing main gate logic. No alternate generator, threshold, estimator, SSL or T025 is authorized.
