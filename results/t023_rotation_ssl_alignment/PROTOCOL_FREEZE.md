# T023 protocol freeze

Lead89466b3;2026-09-14. Rotation SSL alignment audit only. Reuse T022/T021 checkpoint, T007R banks/states, T009 natural K20 IDs, canonical H512, deterministic corruption, T014 utility and historical opposite-half integer counts. No model/state/BN update, gradient writer, semantic observer, new representation or federation.

Reuse map: t021_features.extract owns the frozen H forward; src/data/covariate.py owns corruption; the existing PFLlib loader supplies only x; T023 never accesses its y field. New rotation_probe owns only SHA folds, synthetic labels and disposable SVD least-squares fits. Existing exact utility/count scoring is opened only after the complete independent Phase-A freeze. T022's133 tests and independent H/state audit are the donor baseline; new focused probe tests passed before extraction changes.

Rotations are torch.rot90 after corruption, r=0,1,2,3, without interpolation. Reuse all5000 unrotated H paths and replay5000 samples (clients0,24,49,74,99; both banks/all contexts/states). Newly infer only r1..3. Store per-client H[bank/context][state,image,rotation,512], unrotated argmax and post-corruption pixels remotely with hashes; no class labels or disposable coefficients saved.

Fold order is ascending full SHA256(T023|fold|client|original_support_id), first10 A/rest10 B; reuse across the entire grid. Design is raw float64 H plus a bias coordinate. Minimum-norm SVD retains singular values strictly greater than1e-12 times largest. Rotation targets are4-way one-hot. Score convention fixed here: mean squared error over all40 held-out views and all4 output coordinates, averaged across A→B/B→A. No normalization or regularization. Exact numerical score ties use first canonical state. Same clean-context choice supplies CLEAN-SURROGATE for all shifted contexts.

Exactly16 per-image label permutations: full big-endian SHA256(T023|scramble|client|original_support_id|replica) into PCG64; permutation(4). For canonical clean state, fit/evaluate the same two directions. Report per-cell true score, scramble median/p05/p95 and true<median; by bank/context also report mean true score, quantiles across16 client-mean scramble scores and fraction of100 clients beating their own scramble median. This is diagnostic only.

Independent preflight before privileged scoring: direct original-model H pre-hook replay>=5000 unrotated samples and>=2000 rotated sample features/tensors; explicit quarter-turn equality; all folds10/10 disjoint; independent numpy.linalg.lstsq reconstruction>=2000 cross-fitted state scores (>=4000 fits), selected deterministically; all5 scores/choices/ties; all16 scramble replicates on>=200 cells. Require score error<=1e-10 and finite coefficients. A mismatch produces T023-I and stops without tuning. Complete choice table/folds/feature hashes and explicit sealed-label/utility/query/update flags are frozen before evaluator use.

ROT-TASK-A inherits>=.8 capture bothbanks/allsalts in>=3/4shifts and all shifted rows>=BBSE-S-01 minus.5pp. ROT-REGRET-A vsP and ROT-CTX-A vsCLEAN each require>=15% mean regret reduction and p90<=1.05baseline, allbank/saltrows in>=3/4shifts. Zero baseline mean is noneligible as inT021/T022. Spearman uses average ranks over the five states; constant vectors are undefined and reported separately, never replaced by zero. Report per-bank/context median/IQR/positive fraction over defined correlations, with undefined counts and salt/half detail retained.

Outcomes: R bothTASK/REGRETpass; C onlyTASKpass; F bothfail; Xremainingmixed. CTXflagseparate. No rescue/alternateSSL/writer/T024; return to Lead. Compact receipts committed, large NPZ remote with path/size/SHA manifest. Increment checks: focused probe tests→full remote suite+extraction→independent preflight→frozen integer scorer and all-gate verification.
