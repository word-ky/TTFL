# T019 protocol frozen before execution

Lead `d37bbc4`; same T018R2 direct-RHS CLS, tolerances, checkpoint, K20, contexts, states, template fractions and P00 denominator. This is a privileged diagnostic with true support labels, not a deployable estimator. Zero new forwards planned.

- Reconstruct per-example float64 softmax with the unchanged T016 function from T015 frozen logits. Replay soft numerator/denominator and target exclusion exactly; require actual q and per-class residual identity max-abs <=1e-12. Absent classes remain undefined, not imputed.
- Each null uses 128 replicas, exactly the observed n_y, and replacement as T017. Pool order is source client 0..99, then frozen support position, excluding target. Independent PCG64 generator seed is the full SHA256 integer (big endian) of UTF-8 `T019|client|bank|context|replica|class`. Save each selected flat source index and a mapping to source client/support position/original ID.
- For shifted paired nulls, use the shifted draw's exact source IDs to fetch both shifted and clean posteriors. Do not reuse independent clean draws from Phase B. Real clean/shift use identical frozen target supports; verify full coverage and normalize by n_y/20. Compare norm1 of the weighted vector difference; also save norm2 and signed task-DU/regret changes (shift minus paired clean).
- For client-level task metrics, average the two held-out-template halves within each salt, separately for each replica. Compute null percentiles after this averaging. Keep all four salts separate. Strictly above linear-interpolated p95, not >=p95. DU-or-regret means the union of exceedance indicators for that client within a salt.
- TASK context: require >=20% union exceedance in both banks for every salt; TASK-MISMATCH-A requires >=2/3 Contrast/Noise/Blur. CTX context: same all-salt/both-bank rule, union of paired residual-L1 and signed delta-DU exceedance; >=2/3. Report each individual metric and each salt so stability is visible.
- LOCAL per context/salt/bank: only clients with positive mean exact true-template regret enter denominator. Rank the best observed class by exact rational reduction in the mean regret of two halves, tie by smallest class. Divide by the real-to-all-repaired regret gap without clipping negative improvements. Exact-zero gaps are separate/undefined. Require >=50% recovery for >=25% eligible clients, both banks, all salts; also report each salt separately.
- For the qualitative generic branch, call clean excess substantial only when the same >=20% client tail criterion holds for residual-L1 or task DU/regret, both banks/all salts. Otherwise report mixed evidence rather than forcing G. S versus X additionally requires descriptive utility-margin/state-boundary evidence; report fixed margin quartiles and ties, with no deployment threshold or estimator tuning.
- One-class repair is q_real minus its weighted residual contribution. All-class repair is exactly C@pi_true and must pass the existing noise-free/optimal-set sanity. No label-based repair is used as a deployment policy.
- Hash posterior arrays, residuals, source IDs, solver/utility choices, null percentiles, class repair attribution and paired statistics before opening any query-count arrays. Only then add capture diagnostics from frozen counts.
- Client-level capture is a contribution to aggregate macro-class gain: normalize class hits by the unchanged global per-class query totals, then divide its gain over zero by the client's canonical-P00 contribution gain. Nonpositive individual denominators are marked undefined and excluded from client capture percentiles; preserve their counts. Aggregate capture uses the historical exact global P00 denominator unchanged. This avoids silently treating zero/negative individual denominators as valid capture ratios.

Source artifacts T015/T016/T017/T018R2 remain unchanged. No feature experiment, SSL/TTT, new calibration, new operator or FL.
